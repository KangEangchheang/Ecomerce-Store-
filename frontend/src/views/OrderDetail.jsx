function OrderDetail() {
    return ( 
        <div>
            this should only be access only after user click order in the cart page and the param must have an id
        </div>
     );
}

export default OrderDetail;